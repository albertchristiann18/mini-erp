#!/bin/bash

# Match CI: provide a fallback SECRET_KEY so mypy doesn't crash on Django plugin load
export SECRET_KEY="${SECRET_KEY:-ci-precommit-secret-key-do-not-use-in-production}"

uv run mypy --config-file mypy.ini .
